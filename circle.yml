checkout:
  post:
    - git submodule update --init
    - ./applyPatches.sh
dependencies:
  override:
    - mvn install
test:
  override:
    - echo 'skipping'
general:
  artifacts:
    - "Kyoka-API/target/kyoka*"
    - "Kyoka-Server/target/kyoka*"
